// Re-export skeleton components from the main UI library for backwards compatibility
// This ensures existing admin pages continue to work without changes
export {
  TableSkeleton,
  CardSkeleton,
  StatCardSkeleton,
  PageHeaderSkeleton,
} from '@/components/ui/skeleton'
